% !TeX compiler = xelatex
% !TeX encoding = UTF-8

\documentclass[a4paper, 12pt]{article}

% ==========================================
%   1. 基础宏包与字体设置
% ==========================================
\usepackage[UTF8, heading=true]{ctex} % 中文支持
\usepackage{geometry}       % 页面布局
\usepackage{fancyhdr}       % 页眉页脚
\usepackage{graphicx}       % 图片支持
\usepackage{xcolor}         % 颜色支持
\usepackage{tikz}           % 绘图支持
\usetikzlibrary{shadows}    % 【修复】加载 shadows 库以防万一
\usepackage{listings}       % 代码环境
\usepackage[most]{tcolorbox}% 现代盒子
\usepackage{booktabs}       % 专业表格
\usepackage{hyperref}       % 超链接
\usepackage{enumitem}       % 列表定制
\usepackage{float}          % 浮动控制
\usepackage{mathpazo}       % 使用 Palatino 字体
\usepackage{caption}        % 图表标题美化
\usepackage{titletoc}       % 目录美化
\usepackage{multirow}
% ==========================================
%   2. 视觉风格定义
% ==========================================
% --- 颜色定义 ---
\definecolor{ThemeColor}{RGB}{0, 51, 102}    % 主题色：厦大蓝
\definecolor{LightBlue}{RGB}{235, 242, 250}  % 浅蓝背景
\definecolor{CodeBg}{RGB}{250, 250, 250}     % 代码背景：极浅灰
\definecolor{LineNumBg}{RGB}{240, 240, 240}  % 行号背景
\definecolor{CodeGreen}{RGB}{80, 161, 79}    % 代码注释绿
\definecolor{CodePurple}{RGB}{166, 38, 164}  % 代码关键字紫
\definecolor{CodeBlue}{RGB}{64, 120, 242}    % 代码函数蓝

% --- 页面布局 ---
\geometry{left=2.5cm, right=2.5cm, top=3cm, bottom=2.5cm, headheight=25pt}

% --- 链接设置 ---
\hypersetup{
    colorlinks=true,
    linkcolor=ThemeColor,
    filecolor=ThemeColor,      
    urlcolor=ThemeColor,
    pdftitle={操作系统实验报告},
}

% --- 章节标题设置 (ctex) ---
\ctexset{
    section = {
        format = \Large\bfseries\color{ThemeColor},
        beforeskip = 24pt,
        afterskip = 12pt
    },
    subsection = {
        format = \large\bfseries\color{ThemeColor!80!black},
    }
}

% --- 图表标题设置 ---
\DeclareCaptionFont{ThemeColor}{\color{ThemeColor}}
\captionsetup{
    labelfont={bf, ThemeColor},
    textfont={bf},
    labelsep=quad
}

% --- 页眉页脚 ---
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\bfseries\color{ThemeColor} 操作系统实验报告}
\fancyhead[R]{\small\leftmark}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{1pt}
\renewcommand{\headrule}{\hbox to\headwidth{\color{ThemeColor}\leaders\hrule height \headrulewidth\hfill}}

% --- 目录样式美化 ---
\titlecontents{section}[0pt]{\addvspace{10pt}\bfseries}
{\color{ThemeColor}\thecontentslabel\hspace{1em}}
{}
{\titlerule*[8pt]{.}\contentspage}

\titlecontents{subsection}[2em]{\addvspace{3pt}}
{\thecontentslabel\hspace{1em}}
{}
{\titlerule*[8pt]{.}\contentspage}


\newtcblisting{codeblock}[2][]{%
    enhanced, breakable, listing engine=listings, listing only,%
    title={#2}, coltitle=black!70, fonttitle=\small\bfseries\sffamily,%
    attach boxed title to top left={xshift=22mm, yshift=-4mm},%
    boxed title style={empty, size=minimal, bottom=0pt, top=0pt},%
    colback=CodeBg, colframe=gray!30, boxrule=0.5pt, arc=6mm,%
    left=6mm, right=2mm, top=10mm, bottom=2mm,%
    drop shadow,%
    % 【修复1】使用 underlay 确保背景画在文字下面
    underlay={%
        \begin{tcbclipinterior}
            \fill[LineNumBg] (frame.north west) rectangle ([yshift=-8mm]frame.north east);
            \fill[LineNumBg] (frame.south west) rectangle ([xshift=5.5mm]frame.north west);
        \end{tcbclipinterior}%
    },%
    % 【修复2】overlay 只画装饰点，确保在最上层
    overlay={%
        \node[anchor=west, font=\small\bfseries\sffamily, text=black!70]
        at ([xshift=22mm, yshift=-4mm]frame.north west) {\tcbtitletext};
        \fill[red!70] ([xshift=6mm, yshift=-4mm]frame.north west) circle (2.5pt);
        \fill[orange!70] ([xshift=11mm, yshift=-4mm]frame.north west) circle (2.5pt);
        \fill[green!60!black] ([xshift=16mm, yshift=-4mm]frame.north west) circle (2.5pt);
    },%
    listing options={%
        language=C, basicstyle=\ttfamily\small, keywordstyle=\color{CodePurple}\bfseries,%
        commentstyle=\color{CodeGreen}\itshape, stringstyle=\color{orange!80!black},%
        ndkeywordstyle=\color{CodeBlue}\bfseries, numbers=left, numberstyle=\tiny\color{gray!60}\ttfamily,%
        stepnumber=1, numbersep=10pt, breaklines=true, tabsize=4, showstringspaces=false,%
        frame=none, escapeinside={(*@}{@*)}, columns=fixed, keepspaces=true%
    },%
    #1%
}

% 图片占位宏 (修复版)
\newcommand{\placeholderimg}[3]{
    \begin{figure}[H]
        \centering
        \begin{tikzpicture}
            % 如果没有 logo 图片，请注释掉下面这一行
            \node[opacity=0.1] {\includegraphics[width=3cm]{img/logo.png}}; 
            \node[align=center] at (0,0) {
                \color{ThemeColor}\bfseries\Large 图片占位符 \\[1em]
                \color{gray}\small 尺寸: #1 $\times$ #2 \\[0.5em]
                \color{black}\itshape #3
            };
        \end{tikzpicture}
        % 【修复点2】移除了自动生成的 label，避免复杂字符报错
        \caption{#3} 
    \end{figure}
}

% ==========================================
%   4. 文档正文
% ==========================================
\begin{document}
    
    % --- 封面 ---
    \begin{titlepage}
        \newgeometry{top=3cm, bottom=2cm, left=2cm, right=2cm}
        \centering
        
        % 顶部装饰
        \begin{tikzpicture}[remember picture, overlay]
            \fill[ThemeColor] (current page.north west) rectangle ([yshift=-1cm]current page.north east);
        \end{tikzpicture}
        
        \vspace*{1cm}
        % Logo (确保 img/logo.png 存在，否则替换为文件名或注释掉)
        \includegraphics[width=8cm]{img/logo.png} 
        
        \vspace{1cm}
        
        {\Huge \bfseries \heiti 操作系统实验报告 \par}
        \vspace{0.5cm}
        {\color{ThemeColor} \rule{0.6\textwidth}{1.5pt}} % 分割线
        \vspace{0.5cm}
        
        {\Large \bfseries 实验三：调度算法比较 \par}
        
        \vspace{1.5cm}
        
        % 信息表格
        \begin{large}
            \renewcommand{\arraystretch}{2.0}
            \begin{tabular}{rc}
                \bfseries 专\hspace{1em}业： & \underline{\makebox[8cm][c]{人工智能}} \\
                \bfseries 姓\hspace{1em}名： & \underline{\makebox[8cm][c]{张晓宇}} \\
                \bfseries 学\hspace{1em}号： & \underline{\makebox[8cm][c]{37220232203927}} \\
                \bfseries 实验日期： & \underline{\makebox[8cm][c]{2025.12.5}} \\
                \bfseries 指导教师： & \underline{\makebox[8cm][c]{曹冬林}} \\
            \end{tabular}
        \end{large}
        
        \vfill
        {\small \today}
    \end{titlepage}
    \restoregeometry
    
    % --- 目录 ---
    {
        \hypersetup{linkcolor=black} 
        \tableofcontents
    }
    \newpage
    
    % --- 正文 ---
    
    \section{实验目的}
    
    本次实验围绕操作系统的核心主题——进程调度算法展开，旨在达成以下目标：
    
    \subsection{深入理解进程调度机制}
    学习操作系统中进程调度器的工作原理，理解调度器如何在多个就绪进程之间进行选择。掌握调度策略与调度算法的区别，以及不同调度算法对系统性能的影响。
    
    \subsection{掌握多种调度算法的原理与实现}
    研究并实现四种经典的进程调度算法：
    \begin{itemize}[label=\textbullet, itemsep=0pt]
        \item \textbf{优先级调度（Priority Scheduling）}：根据进程优先级选择执行，优先级高的进程优先获得CPU。
        \item \textbf{先来先服务（FCFS）}：按照进程到达就绪队列的先后顺序进行调度。
        \item \textbf{轮转调度（Round Robin）}：进程按照循环顺序轮流获得时间片执行。
        \item \textbf{静态多级队列（SML）}：将进程按优先级划分到不同队列，高优先级队列优先调度，队列内部使用轮转策略。
    \end{itemize}
    
    \subsection{设计调度算法评估框架}
    构建一个可切换的调度框架，支持在运行时动态切换调度算法。设计合理的测试方案，包含不同类型的负载（CPU密集型和IO密集型），对调度算法进行公平的性能对比。
    
    \subsection{掌握调度算法性能分析方法}
    学习进程运行时间统计方法，理解就绪时间、运行时间、休眠时间、周转时间等关键指标的含义。通过对比实验，分析不同调度算法在各项指标上的表现差异。
    
    \section{实验环境}
    
    \subsection{硬件环境}
    \begin{itemize}[label=\textbullet, itemsep=0pt]
        \item \textbf{CPU}：Intel i9-13900h
        \item \textbf{GPU}：NVIDIA RTX 4050
        \item \textbf{宿主机操作系统}：Windows 11
    \end{itemize}
    
    \subsection{软件环境}
    \begin{itemize}[label=\textbullet, itemsep=0pt]
        \item \textbf{操作系统}：Ubuntu 18.04 (64位)
        \item \textbf{模拟器}：QEMU
        \item \textbf{编译工具}：gcc, make (build-essential)
        \item \textbf{目标操作系统内核}：xv6
    \end{itemize}
    
    \section{实验要求}
    
    \subsection{任务一：实现调度算法}
    在 \texttt{proc.c} 中编程实现以下四种调度算法：
    \begin{enumerate}
        \item \textbf{优先级调度（Priority Scheduling）}：选择优先级最高（数值最小）的就绪进程执行。
        \item \textbf{先来先服务（FCFS）}：选择创建时间最早的就绪进程执行。
        \item \textbf{轮转调度（Round Robin）}：按循环顺序公平地调度每个就绪进程。
        \item \textbf{静态多级队列（SML）}：按优先级将进程划分为多个队列，高优先级队列优先执行。
    \end{enumerate}
    
    \subsection{任务二：实现测试程序}
    在 \texttt{scheduler\_test.c} 中实现测试程序：
    \begin{enumerate}
        \item 创建 6 个进程，包含 3 个 CPU 密集型进程和 3 个 IO 密集型进程。
        \item 为不同类型的进程设置不同的优先级。
        \item 记录所有进程的运行过程，收集进程统计信息。
        \item 对比各调度算法在平均就绪时间、平均运行时间、平均休眠时间、平均周转时间上的结果。
    \end{enumerate}
    
    \section{实验流程}
    
    实验三完成思路流程图如下：
    
        
    \begin{figure}[H] 
	        \centering
	        \includegraphics[width=1.0\textwidth]{img/experiment_workflow.drawio.pdf}
	        \caption{实验完成流程}
	        \label{fig:scheduler_selection}
   	\end{figure}
    
    \subsection{实验准备：调度框架设计}
    本次实验需要设计一个可扩展的调度框架，支持在运行时动态切换调度算法。同时需要扩展进程控制块以支持运行时间统计。
    
    \subsubsection{扩展进程控制块}
    为了支持调度算法比较，需要在 \texttt{proc.h} 的进程控制块中添加以下时间统计字段：
    \begin{itemize}
        \item \texttt{priority}：进程优先级 (1-20)
        \item \texttt{ctime}：进程创建时间
        \item \texttt{stime}：休眠(SLEEPING)累计时间
        \item \texttt{retime}：就绪(RUNNABLE)累计时间
        \item \texttt{rutime}：运行(RUNNING)累计时间
    \end{itemize}
    
    \subsubsection{调度框架设计}
    在 \texttt{sdh.h} 中设计可扩展的调度框架，核心思想是使用函数指针数组存储各调度算法，通过 \texttt{setscheduler()} 系统调用动态切换当前使用的调度函数。
    
    调度框架的设计思路如图\ref{fig:sched_framework}所示。
    
%    \begin{figure}[H] 
%        \centering
%        \includegraphics[width=0.7\textwidth]{img/sched_framework.drawio.pdf}
%        \caption{调度框架设计流程}
%        \label{fig:sched_framework}
%    \end{figure}
    
    \subsubsection{就绪队列实现}
    
    为了提高调度效率，实现了真正的就绪队列数据结构。
    
    \textbf{队列与堆结构设计：}
    
    在 \texttt{proc.c} 中定义了不同的数据结构支持各种调度算法：
    \begin{itemize}
        \item \texttt{fcfsQueue}：用于 FCFS 和 RR 调度的先进先出队列（1个队列）
        \item \texttt{pHeap}：用于优先级调度的\textbf{最小堆}结构，时间复杂度 O(log n)
        \item \texttt{smlQueues[3]}：用于静态多级队列调度（3个队列：高/中/低）
    \end{itemize}
    
    \textbf{最小堆优化说明：}
    
    优先级调度采用最小堆而非多级队列实现，具有以下优势：
    \begin{itemize}
        \item 插入进程：O(log n)，通过 \texttt{siftUp()} 上浮操作维护堆性质
        \item 提取最高优先级进程：O(log n)，通过 \texttt{siftDown()} 下沉操作
        \item 堆的性质：父节点优先级 $\leq$ 子节点优先级（数值越小优先级越高）
    \end{itemize}
    
    \textbf{核心实现：}
    
    \begin{codeblock}{enqueue}
    // 入队：将进程添加到队列尾部
        void enqueue(struct procqueue *q, struct proc *p) {
        p->next = 0;
        if (q->tail == 0) {
            q->head = q->tail = p;
        } else {
            q->tail->next = p;
            q->tail = p;
        }
        }
    \end{codeblock}
    
    \begin{codeblock}{dequeue}
// 出队：从队列头部取出进程
struct proc *dequeue(struct procqueue *q) {
    struct proc *p = q->head;
    if (p == 0)
    return 0;
    q->head = p->next;
    if (q->head == 0) {
    q->tail = 0;
    }
    p->next = 0;
    return p;
}
    \end{codeblock}
    
        \begin{codeblock}{addToReadyQueues}
        
        // 统一入队函数：进程变为 RUNNABLE 时调用
        // 根据当前激活的调度器，只添加到对应的队列
        extern int schedSelected; // 来自 sdh.h，表示当前调度器类型
        
        void addToReadyQueues(struct proc *p) { // 将进程p加入到就绪队列
        switch (schedSelected) {
            case 0: // DEFAULT - 使用 fcfsQueue
            case 2: // FCFS
            case 3: // RR (CFS)
            enqueue(\&fcfsQueue, p);
            break;
            case 1: // PRIORITY - 使用最小堆
            heapInsert(p);
            break;
            case 4: // SML
            if (p->priority >= 1 \&\& p->priority <= 7) {
            enqueue(\&smlQueues[0], p);
            } else if (p->priority >= 8 \&\& p->priority <= 14) {
            enqueue(\&smlQueues[1], p);
            } else {
            enqueue(\&smlQueues[2], p);
            }
            break;
            default:
            enqueue(\&fcfsQueue, p);
            break;
        }
        }
    \end{codeblock}
    \textbf{调度器选择机制：}
    
    调度器选择通过编译时宏定义和运行时函数指针两种机制实现：
    \begin{enumerate}
        \item \textbf{编译时}：通过 Makefile 传递 \texttt{SCHEDPOLICY} 宏，在 \texttt{sdh.h} 中初始化 \texttt{ready\_process} 函数指针
        \item \textbf{运行时}：通过 \texttt{setscheduler(sid)} 系统调用动态切换调度器，同时重建就绪队列
    \end{enumerate}
    
    调度器选择机制流程如图\ref{fig:scheduler_selection}所示。
    
%    \begin{figure}[H] 
%        \centering
%        \includegraphics[width=0.9\textwidth]{img/scheduler_selection_flow.png}
%        \caption{调度器选择机制流程}
%        \label{fig:scheduler_selection}
%    \end{figure}
    
    \subsubsection{进程统计信息收集}
    实现 \texttt{update\_statistics()} 函数，在每个时钟中断时被调用，根据当前进程状态更新对应的时间计数器。该函数遍历进程表，根据进程的 SLEEPING、RUNNABLE、RUNNING 状态分别递增 \texttt{stime}、\texttt{retime}、\texttt{rutime} 计数器。
    
    \subsection{任务一：实现调度算法}
    
    \subsubsection{调度器主循环}
    修改 \texttt{scheduler()} 函数，使用函数指针 \texttt{ready\_process} 调用当前选择的调度算法，实现调度策略的动态切换。
    
   \textbf{ 调度器函数：}
    
    \begin{codeblock}{scheduler}
    // scheduler - 调度器主循环函数
    // 功能：操作系统的核心调度循环，负责选择并切换到下一个要执行的进程
    // 说明：此函数永不返回，每个CPU核心启动后会一直在此循环中运行
    void scheduler(void) {
    struct proc *p;          // 指向被选中进程的指针
    struct cpu *c = mycpu(); // 获取当前CPU结构体
    c->proc = 0;             // 初始化：当前CPU没有运行任何进程
    
    for (;;) { // 无限循环：调度器永不停止
        // 步骤1：启用中断
        sti(); // SeT Interrupt flag（设置中断标志）主要目的：
        // 若无就绪进程，CPU会空转，设置中断标志后时钟中断可以触发，从而调用wakeup()。否则会死锁
        
        // 步骤2：获取进程表锁，准备查找就绪进程
        acquire(\&ptable.lock);
        
        // 步骤3：调用当前选定的调度算法选择下一个进程
        acquire(\&schedulerlock);
        p = (*ready_process)(); // 调用调度算法函数（如priorityScheduler、fcfsScheduler等）
        release(\&schedulerlock);
        
        // 步骤4：如果找到了就绪进程，则进行上下文切换
        if (p != 0) {
        // 4.1 设置当前CPU正在运行的进程
        c->proc = p;
        
        // 4.2 切换到进程的页表（用户地址空间）
        switchuvm(p);
        
        // 4.3 将进程状态标记为RUNNING
        p->state = RUNNING;
        
        // 4.4 执行上下文切换：保存调度器上下文，恢复进程上下文
        swtch(\&(c->scheduler), p->context);
        
        // 4.5 进程p切换回来后，恢复内核页表
        switchkvm();
        
        // 4.6 清理：当前CPU不再运行任何进程
        c->proc = 0;
        }
        
        // 步骤5：释放进程表锁
        release(\&ptable.lock);
    }
    }
    \end{codeblock}
    
    调度器主循环流程如图\ref{fig:scheduler_main}所示。
    
    \begin{figure}[H] 
        \centering
        \includegraphics[width=0.8\textwidth]{img/scheduler_main.drawio.pdf}
        \caption{调度器主循环流程}
        \label{fig:scheduler_main}
    \end{figure}
    
    \subsubsection{优先级调度算法（Priority Scheduling）}
    
    \textbf{算法原理：}
    
    优先级调度是一种基于进程重要性的抢占式或非抢占式调度算法。每个进程被分配一个优先级值，调度器总是选择优先级最高的就绪进程执行。在本实现中，优先级使用数值1-20表示，\textbf{数值越小表示优先级越高}。
    
    该算法的核心思想是：\textit{重要的任务应该优先得到CPU资源}。通过为不同类型的进程分配不同的优先级，系统可以确保关键任务（如系统服务、实时任务）能够快速响应，而次要任务（如后台批处理）则在资源空闲时执行。
    
    \textbf{实现策略：}
    
    本实现采用\textbf{最小堆}数据结构实现非抢占式优先级调度，具体设计如下：
    \begin{enumerate}[itemsep=2pt]
        \item 使用结构体 \texttt{priorityHeap} 存储就绪进程指针数组和堆大小
        \item 插入时调用 \texttt{heapInsert()}，将进程放入堆末尾后执行 \texttt{siftUp()} 上浮
        \item 提取时调用 \texttt{heapExtract()}，取出堆顶后将末尾元素放到堆顶，执行 \texttt{siftDown()} 下沉
        \item 堆顶始终是优先级最高（数值最小）的进程
    \end{enumerate}
    
    相比遍历进程表的 O(n) 实现，最小堆将提取操作优化到 O(log n)。
    \begin{enumerate}[itemsep=2pt]
        \item 遍历整个进程表，筛选出所有状态为 RUNNABLE 的就绪进程
        \item 维护一个候选进程指针 \texttt{highP}，初始值为 NULL
        \item 对每个就绪进程，比较其 \texttt{priority} 字段与当前候选进程
        \item 如果发现更高优先级的进程（数值更小），则更新候选进程指针
        \item 遍历完成后，返回优先级最高的进程
    \end{enumerate}
    
    \textbf{算法优势：}
    \begin{itemize}[itemsep=0pt]
        \item \textbf{响应快速}：高优先级进程可以立即获得CPU，适合实时系统
        \item \textbf{灵活性高}：可以根据实际需求动态调整进程优先级
        \item \textbf{实现简单}：只需一次遍历即可完成调度决策
    \end{itemize}
    
    \textbf{潜在问题：}
    \begin{itemize}[itemsep=0pt]
        \item \textbf{饥饿问题}：低优先级进程可能永远得不到执行机会
        \item \textbf{不够公平}：相同优先级的进程之间没有轮转机制
    \end{itemize}
    
    优先级调度算法的实现流程如图\ref{fig:priority_sched}所示。
    
%    \begin{figure}[H] 
%        \centering
%        \includegraphics[width=0.7\textwidth]{img/priority_scheduler.drawio.pdf}
%        \caption{优先级调度算法实现流程}
%        \label{fig:priority_sched}
%    \end{figure}
    
    \textbf{核心代码实现：}
    
    优先级调度算法的核心代码如下。
    \begin{codeblock}{priorityScheduler}
        // Priority Scheduler -------------------
        // 使用最小堆提取优先级最高（数值最小）的 RUNNABLE 进程
        // 时间复杂度 O(log n)
        struct proc *priorityScheduler() {
        return heapExtract();  // 从最小堆中提取堆顶（优先级最高的进程）
        }
    \end{codeblock}

    
    \subsubsection{先来先服务调度算法（FCFS）}
    
    \textbf{算法原理：}
    
    先来先服务（First-Come First-Served）是最简单直观的调度算法，遵循"先到先得"的公平原则。进程按照进入就绪队列的时间顺序排队，调度器总是选择最早到达的进程执行，直到该进程执行完毕或主动放弃CPU。
   
    
    \textbf{实现策略：}
    
    \begin{codeblock}{fcfsScheduler}
        // FCFS Scheduler -----------------------
        struct proc *fcfsScheduler() { // 先来先服务
        return dequeue(\&fcfsQueue);  // 从就绪顺序队列中取出
        }
    \end{codeblock}
    
    
    \textbf{算法优势：}
    \begin{itemize}[itemsep=0pt]
        \item \textbf{实现简单}：无需复杂的数据结构和调度决策逻辑
        \item \textbf{公平性好}：所有进程按到达顺序依次执行，不会饥饿
        \item \textbf{可预测性强}：进程等待时间可以根据队列长度估算
    \end{itemize}
    
    \textbf{潜在问题：}
    \begin{itemize}[itemsep=0pt]
        \item \textbf{护航效应（Convoy Effect）}：一个长进程会阻塞后面所有短进程
        \item \textbf{平均等待时间长}：短作业可能因为前面的长作业而等待很久
        \item \textbf{不适合交互式系统}：无法优先响应用户交互请求
    \end{itemize}
    
    FCFS 调度算法原理如图\ref{fig:FIFS}所示。
    
    \begin{figure}[H] 
        \centering
        \includegraphics[width=0.85\textwidth]{img/FIFS.jpeg}
        \caption{先来先服务调度原理}
        \label{fig:FIFS}
    \end{figure}
    
    
    \subsubsection{轮转调度算法（Round Robin）}
    
    \textbf{算法原理：}
    
    轮转调度（Round Robin，简称RR）是一种\textbf{时间片轮转}的抢占式调度算法，被广泛应用于分时操作系统中。其核心思想是：将CPU时间划分为固定长度的时间片（time quantum），就绪队列中的进程按\textbf{循环顺序}依次获得一个时间片的执行机会。
    
    当一个进程的时间片用完后，即使它尚未完成，也会被迫让出CPU，回到就绪队列末尾等待下一轮调度。这种机制保证了所有进程都能\textit{公平地}获得CPU资源，不会出现某个进程长期占用CPU的情况。
    
    \textbf{实现策略：}
    
    \begin{codeblock}{rrScheduler}
        // Round Robin Scheduler -------------------------
        struct proc *rrScheduler() {  // 轮转调度
        return dequeue(\&fcfsQueue); // 从FCFS队列取出，调度后由yield重新入队尾
        // 当前文件中与先来先服务的实现相同，但是在trap.c中，只有轮转是抢占式，即只有轮转会被时钟中断抢占
        }
    \end{codeblock}
    
    \textbf{时间片设置：}在xv6中，时间片由时钟中断频率决定，默认为10ms。\textbf{只有 RR 调度器}在时钟中断时会强制调用 \texttt{yield()} 让出CPU，其他调度器（FCFS、PRIORITY、SML）均为非抢占式。
    
    \textbf{算法优势：}
    \begin{itemize}[itemsep=0pt]
        \item \textbf{公平性极佳}：所有进程获得相同的CPU时间配额
        \item \textbf{响应时间好}：交互式任务不会等待太久
        \item \textbf{无饥饿问题}：每个进程都保证在有限时间内得到调度
        \item \textbf{适合分时系统}：特别适合多用户、多任务环境
    \end{itemize}
    
    \textbf{潜在问题：}
    \begin{itemize}[itemsep=0pt]
        \item \textbf{上下文切换开销}：频繁切换进程会增加系统开销
        \item \textbf{时间片难以选择}：过长影响响应时间，过短增加开销
        \item \textbf{不区分优先级}：无法优先服务重要进程
    \end{itemize}
    
    轮转调度算法原理如图\ref{fig:RR}所示。
    
    \begin{figure}[H] 
        \centering
        \includegraphics[width=0.7\textwidth]{img/RoundRobin.jpg}
        \caption{轮转调度原理}
        \label{fig:RR}
    \end{figure}
    
    
    \subsubsection{静态多级队列调度算法（SML）}
    
    \textbf{算法原理：}
    
    静态多级队列（Static Multi-Level Queue）是一种\textbf{混合型调度算法}，它巧妙地结合了优先级调度和轮转调度的优点。该算法的核心思想是：根据进程的优先级将其\textit{永久性地}分配到不同的就绪队列中，高优先级队列总是优先得到调度，而队列内部则采用轮转策略保证公平性。
    
    "静态"体现在进程一旦被分配到某个队列，其所属队列关系就固定不变；"多级"则体现在系统维护多个不同优先级的就绪队列。这种设计既保证了重要进程的响应速度，又避免了同等重要进程之间的不公平竞争。
    
    \textbf{队列划分策略：}
    
    本实现将进程按优先级划分为三个队列：
    \begin{itemize}[itemsep=2pt]
        \item \textbf{队列1（高优先级）}：priority 1-7 \quad （系统服务、实时任务）
        \item \textbf{队列2（中优先级）}：priority 8-14 \quad （普通用户进程）
        \item \textbf{队列3（低优先级）}：priority 15-20 \quad （后台批处理）
    \end{itemize}
    
     静态多级队列调度算法原理如图\ref{fig:SML}所示。
    
    \begin{figure}[H] 
        \centering
        \includegraphics[width=0.7\textwidth]{img/SML.png}
        \caption{静态多级队列调度原理}
        \label{fig:SML}
    \end{figure}
    
    \textbf{实现策略：}
    
    调度器按照以下严格顺序选择进程：
    \begin{enumerate}[itemsep=2pt]
        \item \textbf{优先调度队列1}：使用轮转方式从高优先级队列中选择进程
        \item 如果队列1为空，则调度队列2：同样使用轮转策略
        \item 如果队列2也为空，才调度队列3：继续使用轮转策略
        \item 每个队列维护独立的 \texttt{lastIdx} 索引，记录上次调度位置
        \item 当高优先级队列中有新进程就绪时，立即抢占低优先级队列的CPU
    \end{enumerate}
    
    \textbf{算法优势：}
    \begin{itemize}[itemsep=0pt]
        \item \textbf{兼顾效率与公平}：高优先级进程快速响应，同级进程公平竞争
        \item \textbf{调度开销较低}：只需遍历当前优先级队列，不必扫描全部进程
        \item \textbf{易于实现}：逻辑清晰，代码结构简单
        \item \textbf{灵活可扩展}：可根据需要增加或减少队列数量
    \end{itemize}
    
    \textbf{潜在问题：}
    \begin{itemize}[itemsep=0pt]
        \item \textbf{低优先级饥饿}：如果高优先级队列持续有进程，低优先级进程可能长期得不到调度
        \item \textbf{队列边界敏感}：优先级7和8的进程可能表现差异很大
        \item \textbf{缺乏动态调整}：无法根据进程行为自动提升或降低优先级
    \end{itemize}
    
    \textbf{改进空间：}可以引入\textit{老化机制}（aging），当低优先级进程等待时间过长时，临时提升其优先级，避免饥饿问题。
    
%    \begin{figure}[H] 
%        \centering
%        \includegraphics[width=1.0\textwidth]{img/sml_scheduler.drawio.pdf}
%        \caption{静态多级队列调度算法实现流程}
%        \label{fig:sml_sched}
%    \end{figure}
    
    算法实现如下图所示。
    \begin{codeblock}{smlScheduler}
        // SML (Static Multi-Level Queue) Scheduler ---------------------------
        // 依次从高、中、低优先级队列出队，实现分层调度
        // 高优先级队列优先，同级别内 FCFS
        // 时间复杂度 O(1)
        struct proc *smlScheduler() {
        struct proc *p;
        
        // 优先从高优先级队列出队
        p = dequeue\&smlQueues[0]);
        if (p != 0) return p;
        
        // 高优先级队列空，尝试中优先级队列
        p = dequeue(\&smlQueues[1]);
        if (p != 0) return p;
        
        // 中优先级队列也空，尝试低优先级队列
        return dequeue(\&smlQueues[2]);
        }
    \end{codeblock}
    
    \subsection{任务二：实现测试程序}
    
    \subsubsection{测试程序设计思路}
    
    测试程序实现思路流程图如下：
    
    \begin{figure}[H] 
    	\centering
    	\includegraphics[width=1.0\textwidth]{img/scheduler_test.drawio.pdf}
    	\caption{静态多级队列调度原理}
    	\label{fig:test}
    \end{figure}
    
    为了全面对比不同调度算法的特性，测试程序 \texttt{scheduler\_test.c} 设计了\textbf{三个典型测试场景}，分别针对调度算法的不同弱点进行测试：
    
    \textbf{场景1：纯CPU长短作业（车队效应测试）}
    \begin{itemize}[itemsep=2pt]
        \item 创建2个长作业（CPU\_WORK）+ 2个短作业（CPU\_WORK/10）+ 2个中等作业（CPU\_WORK/3）
        \item 所有进程优先级相同（priority=10），排除优先级干扰
        \item 短作业比长作业晚10个时钟周期创建，模拟作业错峰到达
        \item \textbf{测试目的}：展示FCFS的护航效应（Convoy Effect）以及RR的公平性优势
    \end{itemize}
    
    \textbf{场景2：交互型vs背景任务（响应性测试）}
    \begin{itemize}[itemsep=2pt]
        \item 2个CPU背景任务（低优先级18，执行大量计算）
        \item 2个交互型任务（高优先级3，短CPU计算 + 频繁sleep模拟用户等待）
        \item 2个IO背景任务（中优先级12，文件IO + sleep）
        \item \textbf{测试目的}：展示RR/SML调度器对交互型进程的响应友好性
    \end{itemize}
    
    \textbf{场景3：优先级饥饿测试（公平性测试）}
    \begin{itemize}[itemsep=2pt]
        \item 1个超高优先级大任务（priority=1，CPU\_WORK*2，长期霸占CPU）
        \item 3个中优先级小任务（priority=10）
        \item 2个低优先级小任务（priority=19，可能发生饥饿）
        \item \textbf{测试目的}：展示Priority调度的饥饿问题，以及SML/RR如何缓解
    \end{itemize}
    
    \textbf{命令行参数支持：}
    
    测试程序支持通过命令行参数选择运行场景：
    \begin{itemize}[itemsep=0pt]
        \item \texttt{scheduler\_test} — 运行全部3个场景
        \item \texttt{scheduler\_test 1} — 仅运行场景1（车队效应）
        \item \texttt{scheduler\_test 2} — 仅运行场景2（交互响应）
        \item \texttt{scheduler\_test 3} — 仅运行场景3（优先级饥饿）
    \end{itemize}
    
    每个场景对5种调度算法进行对比测试：DEFAULT、RR（轮转）、PRIORITY、FCFS、SML（多级队列）。测试结果同时输出到屏幕和 \texttt{sched\_result.txt} 文件。
    
    \subsubsection{wait2 系统调用}
    为了收集子进程的运行统计信息，实现 \texttt{wait2()} 系统调用。该系统调用在原有 \texttt{wait()} 的基础上，额外返回子进程的就绪时间、运行时间和休眠时间三个统计指标。
    
    wait2 系统调用的实现流程如图\ref{fig:wait2}所示。
    
    \begin{figure}[H] 
        \centering
        \includegraphics[width=1.0\textwidth]{img/wait2.drawio.pdf}
        \caption{wait2 系统调用实现流程}
        \label{fig:wait2}
    \end{figure}
    
    \subsection{测试与验证}
    
    编译并启动内核 \texttt{make qemu-nox}，运行 \texttt{scheduler\_test} 测试各调度算法。
    
    \textbf{测试配置：}
    
    测试程序按场景分别创建进程：
    \begin{itemize}[itemsep=2pt]
        \item \textbf{场景1}（车队效应）：2长作业 + 2短作业 + 2中作业，同优先级
        \item \textbf{场景2}（交互响应）：2 CPU背景 + 2交互型 + 2 IO背景，差异化优先级
        \item \textbf{场景3}（优先级饥饿）：1高优先级大任务 + 3中优先级 + 2低优先级
    \end{itemize}
    
    \textbf{性能指标解释：}
    \begin{itemize}[itemsep=0pt]
        \item \textbf{就绪时间（Ready Time）}：进程在就绪队列中等待 CPU 的时间
        \item \textbf{运行时间（Run Time）}：进程实际占用 CPU 执行的时间
        \item \textbf{休眠时间（Sleep Time）}：进程因等待 IO 等资源而睡眠的时间
        \item \textbf{周转时间（Turnaround Time）}：进程从创建到完成的总时间（= 就绪 + 运行 + 休眠）
    \end{itemize}
    
    测试运行截图：
        \begin{figure}[H] 
    	\centering
    	\includegraphics[width=0.66\textwidth]{img2/run_result.png}
    	\caption{测试运行截图}
    	\label{fig:run1}
    \end{figure}
    
       \subsubsection{场景1：车队效应分析}
       
       \textbf{预期行为：}
       \begin{itemize}[itemsep=1pt]
           \item \textbf{FCFS}：长作业先执行完，短作业等待时间极长（护航效应）
           \item \textbf{RR}：长短作业交替执行，短作业周转时间显著减少
           \item \textbf{PRIORITY/SML}：同优先级下退化为类似 RR 的行为
       \end{itemize}
       
        测试运行截图：
    \begin{figure}[H] 
    	\centering
    	\includegraphics[width=0.66\textwidth]{img2/run_result1.png}
    	\caption{测试运行截图}
    	\label{fig:run2}
    \end{figure}
       
       \subsubsection{场景2：交互响应分析}
       
       \textbf{预期行为：}
       \begin{itemize}[itemsep=1pt]
           \item \textbf{RR}：交互型进程能快速获得响应，但不考虑优先级
           \item \textbf{PRIORITY/SML}：高优先级交互进程响应最快
           \item \textbf{FCFS}：交互进程可能被 CPU 背景任务阻塞
       \end{itemize}
       
           测试运行截图：
       \begin{figure}[H] 
       	\centering
       	\includegraphics[width=0.66\textwidth]{img2/run_result2.png}
       	\caption{测试运行截图}
       	\label{fig:run3}
       \end{figure}
       
       \subsubsection{场景3：优先级饥饿分析}
       
       \textbf{预期行为：}
       \begin{itemize}[itemsep=1pt]
           \item \textbf{PRIORITY}：低优先级进程饥饿，等待高优先级大任务完成
           \item \textbf{RR}：忽略优先级，所有进程公平获得时间片
           \item \textbf{SML}：队列内轮转，一定程度缓解饥饿
       \end{itemize}
       
           测试运行截图：
       \begin{figure}[H] 
       	\centering
       	\includegraphics[width=0.66\textwidth]{img2/run_result3.png}
       	\caption{测试运行截图}
       	\label{fig:run4}
       \end{figure}
       
    
    \subsubsection{运行结果分析}
    
    % ==================== 场景1：车队效应测试结果 ====================
    
    \textbf{场景1：车队效应测试结果}
    
    \begin{table}[H]
    	\centering
    	\caption{场景1各调度算法平均性能对比（单位：ticks）}
    	\begin{tabular}{lccc|ccc|ccc}
    		\toprule
    		\multirow{2}{*}{\textbf{调度器}} & \multicolumn{3}{c|}{\textbf{长作业}} & \multicolumn{3}{c|}{\textbf{短作业}} & \multicolumn{3}{c}{\textbf{中作业}} \\
    		& 就绪 & 运行 & 周转 & 就绪 & 运行 & 周转 & 就绪 & 运行 & 周转 \\
    		\midrule
    		DEFAULT  & 142 & 220 & 363 & 465 & 22 & 488 & 510 & 75 & 586 \\
    		RR       & 412 & 223 & 635 & 104 & 21 & \textbf{126} & 291 & 73 & 364 \\
    		PRIORITY & 219 & 219 & 438 & \textbf{22} & 22 & \textbf{44} & \textbf{74} & 73 & \textbf{147} \\
    		FCFS     & 415 & 224 & 640 & 126 & 22 & 148 & 270 & 74 & 344 \\
    		SML      & 415 & 224 & 639 & 107 & 22 & 129 & 295 & 74 & 369 \\
    		\bottomrule
    	\end{tabular}
    	\label{tab:scenario1_results}
    \end{table}
    
    \textbf{关键发现：}PRIORITY调度器下短作业周转时间仅44 ticks，远优于其他调度器。这是因为堆结构使短作业能够快速被提取执行。RR/FCFS/SML的短作业周转时间也较短（126-148 ticks），表现出良好的公平性。
    
    % ==================== 场景2：交互响应测试结果 ====================
    
    \textbf{场景2：交互响应测试结果}
    
    \begin{table}[H]
    	\centering
    	\caption{场景2各调度算法平均性能对比（单位：ticks）}
    	\begin{tabular}{lccc|ccc|ccc}
    		\toprule
    		\multirow{2}{*}{\textbf{调度器}} & \multicolumn{3}{c|}{\textbf{交互型}} & \multicolumn{3}{c|}{\textbf{CPU背景}} & \multicolumn{3}{c}{\textbf{IO背景}} \\
    		& 就绪 & 休眠 & 周转 & 就绪 & 运行 & 周转 & 就绪 & 休眠 & 周转 \\
    		\midrule
    		DEFAULT  & 485 & 157 & 723 & 123 & 220 & 343 & 472 & 357 & 876 \\
    		RR       & 399 & 130 & 613 & 447 & 228 & 675 & 439 & 329 & 810 \\
    		PRIORITY & \textbf{16} & 229 & \textbf{325} & 365 & 239 & 604 & 225 & 495 & 763 \\
    		FCFS     & 402 & 133 & 618 & 450 & 231 & 681 & 446 & 316 & 804 \\
    		SML      & 76 & 197 & \textbf{355} & 491 & 240 & 732 & 236 & 409 & 688 \\
    		\bottomrule
    	\end{tabular}
    	\label{tab:scenario2_results}
    \end{table}
    
    \textbf{关键发现：}PRIORITY下交互型进程就绪时间仅16 ticks，周转时间325 ticks；SML下就绪时间76 ticks，周转时间355 ticks。两者都远优于其他调度器，体现了优先级调度对高优先级交互进程的快速响应能力。
    
    % ==================== 场景3：优先级饥饿测试结果 ====================
    
    \textbf{场景3：优先级饥饿测试结果}
    
    \begin{table}[H]
    	\centering
    	\caption{场景3各调度算法平均性能对比（单位：ticks）}
    	\begin{tabular}{lcc|cc|cc}
    		\toprule
    		\multirow{2}{*}{\textbf{调度器}} & \multicolumn{2}{c|}{\textbf{高优先级}} & \multicolumn{2}{c|}{\textbf{中优先级}} & \multicolumn{2}{c}{\textbf{低优先级}} \\
    		& 就绪 & 周转 & 就绪 & 周转 & 就绪 & 周转 \\
    		\midrule
    		DEFAULT  & 41  & 482  & 595 & 707 & 844 & 957 \\
    		RR       & 547 & 1004 & 558 & 667 & 564 & 673 \\
    		PRIORITY & \textbf{1} & \textbf{440} & 367 & 479 & \textbf{207} & \textbf{324} \\
    		FCFS     & 551 & 1010 & 558 & 667 & 573 & 683 \\
    		SML      & \textbf{1} & \textbf{440} & 371 & 484 & 450 & 565 \\
    		\bottomrule
    	\end{tabular}
    	\label{tab:scenario3_results}
    \end{table}
    
    \textbf{关键发现：}
    \begin{itemize}[itemsep=1pt]
    	\item PRIORITY/SML：高优先级进程就绪时间仅1 ticks，响应极快
    	\item PRIORITY：低优先级进程就绪时间仅207 ticks，周转时间324 ticks，表现出乎意料地好（可能因为堆的特性使得进程能较快被调度）
    	\item RR/FCFS：完全忽略优先级，各优先级组就绪时间接近（约550-570 ticks）
    \end{itemize}
    
    % ==================== 综合性能对比表 ====================
    
    \textbf{四、综合性能对比}
    
    \begin{table}[H]
    	\centering
    	\caption{五种调度算法综合性能评估}
    	\begin{tabular}{lccccc}
    		\toprule
    		\textbf{评估维度} & \textbf{DEFAULT} & \textbf{RR} & \textbf{PRIORITY} & \textbf{FCFS} & \textbf{SML} \\
    		\midrule
    		短作业友好性 & 差 & 优 & \textbf{最优} & 优 & 优 \\
    		交互响应性 & 差 & 中 & \textbf{最优} & 中 & 优 \\
    		低优先级公平性 & 差 & 优 & \textbf{优} & 优 & 中 \\
    		高优先级响应 & 中 & 差 & \textbf{最优} & 差 & \textbf{最优} \\
    		\bottomrule
    	\end{tabular}
    	\label{tab:overall_comparison}
    \end{table}
    
    \section{实验总结}
    
    本次实验围绕进程调度算法展开，通过实现和比较多种调度算法，我取得了以下技术成果和理论认识：
    
    \subsection{掌握了调度框架的设计方法}
    我成功设计并实现了一个可扩展的调度框架，通过函数指针实现调度算法的动态切换。这种设计模式使得添加新的调度算法变得简单，只需实现统一接口并注册到函数指针数组即可。
    
    \subsection{深入理解了不同调度算法的特点}
    \begin{itemize}
        \item \textbf{优先级调度}：采用最小堆实现，时间复杂度 O(log n)；高优先级进程能快速响应，但可能导致低优先级进程饥饿
        \item \textbf{FCFS}：真正的非抢占式调度，进程运行直到主动让出 CPU；实现简单但可能导致护航效应
        \item \textbf{轮转调度}：抢占式调度，公平性好，适合交互式系统
        \item \textbf{静态多级队列}：结合了优先级和轮转的优点，在效率与公平之间取得平衡
    \end{itemize}
    
    \subsection{掌握了数据结构优化技术}
    在优先级调度中，我使用最小堆替代了传统的多级队列或线性遍历方式。堆的上浮（siftUp）和下沉（siftDown）操作确保了 O(log n) 的时间复杂度，这是一个重要的算法优化实践。
    
    \subsection{掌握了进程统计信息收集方法}
    通过实现 \texttt{update\_statistics()} 和 \texttt{wait2()} 系统调用，我学会了如何在内核中收集进程的运行时统计信息，这对于分析调度算法性能至关重要。
    
    \subsection{理解了调度决策对系统性能的影响}
    通过对比实验，我直观地观察到不同调度算法在响应时间、周转时间、公平性等方面的差异。这加深了我对"没有万能调度算法"这一理念的理解——选择合适的调度算法需要根据具体应用场景权衡。
    
    \section{实验心得}
    
    这次实验让我对操作系统调度有了更深刻的认识。
    
    首先，我深刻体会到了调度算法的选择对系统行为的深远影响。在实现和测试不同调度算法的过程中，我发现看似简单的调度决策实际上涉及到响应时间、吞吐量、公平性等多个相互制约的因素。例如，优先级调度虽然能让高优先级进程快速响应，但如果不加限制，低优先级进程可能永远得不到执行机会，这就是所谓的"饥饿"问题。
    
    其次，设计可扩展的调度框架让我对软件工程中的抽象和模块化有了新的理解。通过使用函数指针数组，我实现了调度算法的即插即用，这种设计使得添加新算法变得非常简单。这个经验让我意识到，好的架构设计能够极大地降低后续开发和维护的成本。
    
    最后，通过收集和分析进程运行统计数据，我掌握了用数据说话的调试和分析方法。不同调度算法产生的统计数据差异直观地反映了它们的特性，这比单纯阅读理论知识更加印象深刻。
    
    \newpage
    \section{附录：核心代码实现}
    \subsection{附录A：数据结构定义}
    
    \subsubsection{A.1 ptable.h - 进程信息结构}
    
    \begin{codeblock}{ptable.h}
        // ptable.h - 进程信息结构定义，用于内核与用户态程序之间传递进程信息
        #ifndef _PTABLE_H_
        #define _PTABLE_H_
        
        #include "param.h" // 引入NPROC常量定义
        
        // proc_info结构体：轻量级进程快照，专为用户态程序(如ps命令)设计
        struct proc_info {
            int pid;                   // 进程ID，唯一标识一个进程
            int ppid;                  // 父进程ID，如果没有父进程则为-1
            int priority;              // 进程调度优先级(1-20，数值越小优先级越高)
            int mem_size;              // 进程占用的内存大小(字节数)
            enum procstate state;      // 进程状态
            char name[16];             // 进程命令名(最多16字节)
        };
        
        #endif
    \end{codeblock}
    
    \subsubsection{A.2 proc.h - 进程控制块扩展}
    
    \begin{codeblock}{proc.h (关键部分)}
        // 进程状态枚举
        enum procstate { 
            UNUSED,    // 进程槽位未使用
            EMBRYO,    // 进程正在创建中
            SLEEPING,  // 进程因等待某个事件而睡眠
            RUNNABLE,  // 进程就绪，等待被调度执行
            RUNNING,   // 进程正在CPU上运行
            ZOMBIE     // 进程已终止，等待父进程回收
        };
        
        // 进程控制块(PCB)：内核中描述每个进程的核心数据结构
        struct proc {
            uint sz;                     // 进程地址空间大小(字节)
            pde_t* pgdir;                // 进程页目录的虚拟地址
            char *kstack;                // 进程内核栈的底部地址
            enum procstate state;        // 进程当前状态
            int pid;                     // 进程ID
            int priority;                // 新增：进程优先级(1-20)
            struct proc *parent;         // 指向父进程的指针
            struct trapframe *tf;        // 保存用户态寄存器状态
            struct context *context;     // 进程上下文(用于上下文切换)
            void *chan;                  // 睡眠通道
            int killed;                  // 非零表示进程被标记为终止
            struct file *ofile[NOFILE];  // 打开的文件描述符数组
            struct inode *cwd;           // 当前工作目录
            char name[16];               // 进程名称
            int sem_held[32];            // 新增：记录进程持有的信号量资源
        };
    \end{codeblock}
    
    \subsubsection{A.3 proc.c - 信号量结构定义}
    
    \begin{codeblock}{proc.c (信号量定义)}
        // 内核信号量结构：用于进程间的同步与互斥
        struct semaphore {
            int value;              // 信号量的当前值(可用资源数量)
            int active;             // 标志位：1表示该信号量槽位已初始化
            struct spinlock lock;   // 保护value和active字段的自旋锁
        };
        
        // 全局信号量表：系统最多支持32个信号量
        struct semaphore sema[32];
    \end{codeblock}
    
    \subsection{附录B：进程管理系统调用实现}
    
    \subsubsection{B.1 proc.c - setpriority函数}
    
    \begin{codeblock}{setpriority实现}
        // setpriority：修改指定进程的优先级
        // 参数：pid - 目标进程ID，priority - 新优先级值(1-20)
        // 返回值：0表示成功，-1表示失败
        int
        setpriority(int pid, int priority)
        {
            struct proc *p;          // 用于遍历进程表的指针
            int found = 0;           // 标志：是否找到目标进程
            
            // 步骤1：参数校验 - 优先级必须在[1, 20]范围内
            if(priority < 1 || priority > 20)
            return -1;
            
            // 步骤2：获取进程表锁，确保互斥访问
            acquire(&ptable.lock);
            
            // 步骤3：遍历进程表，查找目标进程
            for(p = ptable.proc; p < &ptable.proc[NPROC]; p++){
                if(p->pid == pid){
                    p->priority = priority;  // 修改优先级
                    found = 1;
                    break;
                }
            }
            
            // 步骤4：释放进程表锁
            release(&ptable.lock);
            
            // 步骤5：返回结果
            return found ? 0 : -1;
        }
    \end{codeblock}
    
    \subsubsection{B.2 proc.c - getptable函数}
    
    \begin{codeblock}{getptable实现}
        // getptable：将内核进程表信息复制到用户空间
        // 参数：ubuf - 用户空间缓冲区地址，size - 缓冲区大小
        // 返回值：0表示成功，-1表示失败
        int 
        getptable(void *ubuf, int size)
        {
            struct proc *p;              // 进程表遍历指针
            struct proc_info pi;         // 临时变量，用于构造进程信息
            char *uptr = (char*)ubuf;    // 用户缓冲区指针
            int i = 0;                   // 进程计数器
            
            // 步骤1：缓冲区大小检查
            if(size < NPROC * sizeof(struct proc_info))
            return -1;
            
            // 步骤2：获取进程表锁
            acquire(&ptable.lock);
            
            // 步骤3：遍历进程表
            for(p = ptable.proc; p < &ptable.proc[NPROC]; p++){
                // 清零临时结构体
                memset(&pi, 0, sizeof(pi));
                
                // 如果槽位正在使用，填充信息
                if(p->state != UNUSED){
                    pi.pid = p->pid;
                    pi.ppid = (p->parent) ? p->parent->pid : -1; 
                    pi.priority = p->priority;
                    pi.mem_size = p->sz;
                    pi.state = p->state;
                    safestrcpy(pi.name, p->name, sizeof(pi.name));
                }
                
                // 步骤4：复制到用户空间
                if(copyout(myproc()->pgdir, 
                (uint)(uptr + i * sizeof(struct proc_info)), 
                (char*)&pi, sizeof(pi)) < 0){
                    release(&ptable.lock);
                    return -1;
                }
                i++;
            }
            
            // 步骤5：释放锁并返回
            release(&ptable.lock);
            return 0;
        }
    \end{codeblock}
    
    \subsubsection{B.3 sysproc.c - 系统调用包装器}
    
    \begin{codeblock}{系统调用包装器}
        // sys_setpriority：setpriority系统调用包装器
        int
        sys_setpriority(void)
        {
            int pid, priority;
            
            // 从系统调用参数栈中解析参数
            if(argint(0, &pid) < 0)        // 参数0：进程ID
            return -1;
            if(argint(1, &priority) < 0)   // 参数1：优先级值
            return -1;
            
            // 调用内核实现函数
            return setpriority(pid, priority);
        }
        
        // sys_getptable：getptable系统调用包装器
        int
        sys_getptable(void)
        {
            char *buf;  // 用户空间缓冲区指针
            int size;   // 缓冲区大小
            
            // 解析参数并验证用户地址有效性
            if(argptr(0, &buf, sizeof(struct proc_info) * NPROC) || 
            argint(1, &size))
            return -1;
            
            // 调用内核实现函数
            return getptable(buf, size);
        }
    \end{codeblock}
    
    \subsection{附录C：用户态程序实现}
    
    \subsubsection{C.1 ps.c - 进程查看命令}
    
    \begin{codeblock}{ps.c}
        #include "types.h"
        #include "stat.h"
        #include "user.h"
        #include "param.h"     // 包含NPROC常量定义
        #include "mmu.h"
        #include "proc.h"      // 包含procstate枚举定义
        #include "ptable.h"    // 包含proc_info结构定义
        
        // 进程状态字符串数组：将状态枚举转换为可读字符串
        static char *states[] = {
            [UNUSED]    "UNUSED  ",  // 补空格对齐到8字符
            [EMBRYO]    "EMBRYO  ",
            [SLEEPING]  "SLEEPING",
            [RUNNABLE]  "RUNNABLE",
            [RUNNING]   "RUNNING ",
            [ZOMBIE]    "ZOMBIE  "
        };
        
        int
        main(int argc, char *argv[])
        {
            struct proc_info pinfo[NPROC];  // 接收进程信息的缓冲区
            
            // 步骤1：清零缓冲区，防止打印垃圾数据
            memset(pinfo, 0, sizeof(pinfo));
            
            // 步骤2：调用getptable系统调用获取进程表信息
            if(getptable(pinfo, sizeof(pinfo)) < 0){
                printf(2, "ps: getptable failed\n");
                exit();
            }
            
            // 步骤3：打印表头
            printf(1, "PID    PPID    PRI    MEM    STATE        CMD\n");
            
            // 步骤4：遍历并打印每个进程的信息
            for(int i = 0; i < NPROC; i++){
                // 跳过未使用的进程槽位
                if(pinfo[i].pid == 0)
                continue;
                
                // 打印PID
                printf(1, "%d    ", pinfo[i].pid);
                
                // 打印PPID（-1则显示N/A）
                if(pinfo[i].ppid == -1)
                printf(1, "N/A    ");
                else
                printf(1, "%d    ", pinfo[i].ppid);
                
                // 打印优先级和内存大小
                printf(1, "%d    %d    ", pinfo[i].priority, pinfo[i].mem_size);
                
                // 打印进程状态
                if(pinfo[i].state >= 0 && pinfo[i].state < 6)
                printf(1, "%s    ", states[pinfo[i].state]);
                else
                printf(1, "???    ");
                
                // 打印进程名称
                printf(1, "%s\n", pinfo[i].name);
            }
            
            exit();
        }
    \end{codeblock}
    
    \subsubsection{C.2 nice.c - 优先级修改命令}
    
    \begin{codeblock}{nice.c}
        #include "types.h"
        #include "stat.h"
        #include "user.h"
        #include "fcntl.h"
        
        int
        main(int argc, char *argv[])
        {
            int pid, priority;
            
            // 步骤1：参数检查 - 需要两个参数：pid和priority
            if(argc != 3){
                printf(2, "Usage: nice pid priority\n");
                exit();
            }
            
            // 步骤2：解析命令行参数
            pid = atoi(argv[1]);       // 第一个参数：进程ID
            priority = atoi(argv[2]);  // 第二个参数：优先级值
            
            // 步骤3：参数校验 - 优先级必须在[1, 20]范围内
            if (priority < 1 || priority > 20) {
                printf(2, "nice: priority must be between 1 and 20\n");
                exit();
            }
            
            // 步骤4：调用setpriority系统调用
            if(setpriority(pid, priority) < 0){
                printf(2, "nice: failed to set priority (pid %d not found?)\n", pid);
            } else {
                printf(1, "nice: priority of process %d set to %d\n", pid, priority);
            }
            
            exit();
        }
    \end{codeblock}
    
    \subsection{附录D：信号量实现}
    
    \subsubsection{D.1 proc.c - sem\_init函数}
    
    \begin{codeblock}{sem\_init实现}
        // sem_init：初始化一个信号量
        // 参数：sem - 信号量ID(0-31)，value - 信号量初始值
        // 返回值：0表示成功，-1表示失败
        int
        sem_init(int sem, int value)
        {
            // 步骤1：参数校验
            if(sem < 0 || sem >= 32)
            return -1;
            
            // 步骤2：获取该信号量的锁
            acquire(&sema[sem].lock);
            
            // 步骤3：检查是否已被初始化
            if(sema[sem].active == 1) {
                release(&sema[sem].lock);
                return -1;  // 已在使用，不允许重复初始化
            }
            
            // 步骤4：初始化信号量
            sema[sem].value = value;  // 设置初始值
            sema[sem].active = 1;     // 标记为活跃状态
            
            // 步骤5：释放锁
            release(&sema[sem].lock);
            
            return 0;
        }
    \end{codeblock}
    
    \subsubsection{D.2 proc.c - sem\_destroy函数}
    
    \begin{codeblock}{sem\_destroy实现}
        // sem_destroy：销毁一个信号量
        // 参数：sem - 信号量ID(0-31)
        // 返回值：0表示成功，-1表示失败
        int
        sem_destroy(int sem)
        {
            // 步骤1：参数校验
            if(sem < 0 || sem >= 32)
            return -1;
            
            // 步骤2：获取锁
            acquire(&sema[sem].lock);
            
            // 步骤3：检查是否处于活跃状态
            if(sema[sem].active == 0) {
                release(&sema[sem].lock);
                return -1;
            }
            
            // 步骤4：销毁信号量
            sema[sem].active = 0;  // 标记为未使用
            sema[sem].value = 0;   // 清零计数值
            
            // 步骤5：释放锁
            release(&sema[sem].lock);
            
            return 0;
        }
    \end{codeblock}
    
    \subsubsection{D.3 proc.c - sem\_wait函数(P操作)}
    
    \begin{codeblock}{sem\_wait实现}
        // sem_wait：P操作，申请资源，资源不足则阻塞
        // 参数：sem - 信号量ID，count - 需要申请的资源数量
        // 返回值：0表示成功，-1表示失败
        int 
        sem_wait(int sem, int count)
        {
            // 步骤1：参数校验
            if(sem < 0 || sem >= 32)
            return -1;
            
            // 步骤2：获取信号量锁
            acquire(&sema[sem].lock);
            
            // 步骤3：检查是否处于活跃状态
            if(sema[sem].active == 0){
                release(&sema[sem].lock);
                return -1;
            }
            
            // 步骤4：循环检查资源是否足够（核心逻辑）
            // 使用while而非if，防止虚假唤醒
            while(sema[sem].value < count){
                // 资源不足，进程进入睡眠
                // sleep的参数1：睡眠通道（使用信号量地址）
                // sleep的参数2：当前持有的锁
                // sleep会原子性地释放锁并进入SLEEPING状态
                sleep(&sema[sem], &sema[sem].lock);
                
                // 被唤醒后，回到while开头重新检查条件
                
                // 防御性检查：信号量是否在睡眠期间被销毁
                if(sema[sem].active == 0){
                    release(&sema[sem].lock);
                    return -1;
                }
            }
            
            // 步骤5：资源充足，消耗资源
            sema[sem].value -= count;
            
            // 步骤6：记账，用于进程退出时的自动资源回收
            myproc()->sem_held[sem] += count;
            
            // 步骤7：释放锁
            release(&sema[sem].lock);
            
            return 0;
        }
    \end{codeblock}
    
    \subsubsection{D.4 proc.c - sem\_signal函数(V操作)}
    
    \begin{codeblock}{sem\_signal实现}
        // sem_signal：V操作，释放资源并唤醒等待者
        // 参数：sem - 信号量ID，count - 需要释放的资源数量
        // 返回值：0表示成功，-1表示失败
        int 
        sem_signal(int sem, int count)
        {
            // 步骤1：参数校验
            if(sem < 0 || sem >= 32)
            return -1;
            
            // 步骤2：获取信号量锁
            acquire(&sema[sem].lock);
            
            // 步骤3：检查活跃状态
            if(sema[sem].active == 0){
                release(&sema[sem].lock);
                return -1;
            }
            
            // 步骤4：释放资源，增加信号量的值
            sema[sem].value += count;
            
            // 步骤5：销账，减少当前进程的资源持有计数
            if(myproc()->sem_held[sem] >= count) {
                myproc()->sem_held[sem] -= count;
            } else {
                // 防御性处理：持有计数不足，清零
                myproc()->sem_held[sem] = 0;
            }
            
            // 步骤6：唤醒所有等待该信号量的进程
            // wakeup将所有在该通道上睡眠的进程状态改为RUNNABLE
            wakeup(&sema[sem]);
            
            // 步骤7：释放锁
            release(&sema[sem].lock);
            
            return 0;
        }
    \end{codeblock}
    
    \subsubsection{D.5 sysproc.c - 信号量系统调用包装器}
    
    \begin{codeblock}{信号量系统调用包装器}
        // sys_sem_init：sem_init系统调用包装器
        int
        sys_sem_init(void)
        {
            int sem, value;
            
            if (argint(0, &sem) < 0)      // 解析参数0：信号量ID
            return -1;
            if (argint(1, &value) < 0)    // 解析参数1：初始值
            return -1;
            
            return sem_init(sem, value);
        }
        
        // sys_sem_destroy：sem_destroy系统调用包装器
        int
        sys_sem_destroy(void)
        {
            int sem;
            
            if (argint(0, &sem) < 0)
            return -1;
            
            return sem_destroy(sem);
        }
        
        // sys_sem_wait：sem_wait系统调用包装器
        int 
        sys_sem_wait(void)
        {
            int sem, count;
            
            if (argint(0, &sem) < 0)      // 解析参数0：信号量ID
            return -1;
            if (argint(1, &count) < 0)    // 解析参数1：资源数量
            return -1;
            
            return sem_wait(sem, count);
        }
        
        // sys_sem_signal：sem_signal系统调用包装器
        int 
        sys_sem_signal(void)
        {
            int sem, count;
            
            if (argint(0, &sem) < 0)
            return -1;
            if (argint(1, &count) < 0)
            return -1;
            
            return sem_signal(sem, count);
        }
    \end{codeblock}
    
    \subsection{附录E：信号量测试程序}
    
    \subsubsection{E.1 sem\_test.c - 完整实现}
    
    \begin{codeblock}{sem\_test.c}
        #include "types.h"
        #include "stat.h"
        #include "user.h"
        #include "fcntl.h"
        
        // 测试参数定义
        #define NUM_CHILDREN 10              // 并发子进程数量
        #define TARGET_COUNT_PER_CHILD 50    // 每个子进程的循环次数
        #define COUNTER_FILE "counter"       // 共享计数器文件名
        #define SEM_ID 0                     // 使用0号信号量
        
        // test_counter：信号量互斥测试函数
        void
        test_counter()
        {
            int i, j;
            int fd;
            int val;
            
            // ===== 第一部分：初始化 =====
            
            // 步骤1：创建共享计数器文件并写入初始值0
            fd = open(COUNTER_FILE, O_CREATE | O_RDWR);
            if(fd < 0){
                printf(1, "Error: create counter file failed\n");
                exit();
            }
            val = 0;
            write(fd, &val, sizeof(int));  // 写入4字节整数
            close(fd);
            
            // 步骤2：初始化互斥信号量（初始值为1，作为互斥锁）
            if(sem_init(SEM_ID, 1) < 0){
                printf(1, "Error: sem_init failed\n");
                exit();
            }
            
            printf(1, "Starting %d children, each incrementing %d times...\n", 
            NUM_CHILDREN, TARGET_COUNT_PER_CHILD);
            
            // ===== 第二部分：创建并发子进程 =====
            
            for(i = 0; i < NUM_CHILDREN; i++){
                int pid = fork();
                if(pid < 0){
                    printf(1, "fork failed\n");
                    exit();
                }
                
                if(pid == 0){
                    // --- 子进程代码 ---
                    for(j = 0; j < TARGET_COUNT_PER_CHILD; j++){
                        
                        // P操作：申请进入临界区
                        if(sem_wait(SEM_ID, 1) < 0){
                            printf(1, "Child: sem_wait failed\n");
                            exit();
                        }
                        
                        // ========== 临界区开始 ==========
                        
                        // 读取当前计数值
                        fd = open(COUNTER_FILE, O_RDONLY);
                        if(fd < 0){
                            printf(1, "Child: open read failed\n");
                            exit();
                        }
                        if(read(fd, &val, sizeof(int)) != sizeof(int)){
                            printf(1, "Child: read failed\n");
                            exit();
                        }
                        close(fd);
                        
                        // 更新计数值（加1）
                        val++;
                        
                        // 写回新值
                        fd = open(COUNTER_FILE, O_WRONLY); 
                        if(fd < 0){
                            printf(1, "Child: open write failed\n");
                            exit();
                        }
                        if(write(fd, &val, sizeof(int)) != sizeof(int)){
                            printf(1, "Child: write failed\n");
                            exit();
                        }
                        close(fd);
                        
                        // ========== 临界区结束 ==========
                        
                        // V操作：离开临界区
                        if(sem_signal(SEM_ID, 1) < 0){
                            printf(1, "Child: sem_signal failed\n");
                            exit();
                        }
                    }
                    
                    exit();  // 子进程完成后退出
                }
            }
            
            // ===== 第三部分：等待子进程并验证结果 =====
            
            // 父进程等待所有子进程退出
            for(i = 0; i < NUM_CHILDREN; i++){
                wait();
            }
            
            // 读取最终计数值
            fd = open(COUNTER_FILE, O_RDONLY);
            read(fd, &val, sizeof(int));
            close(fd);
            
            printf(1, "Final counter value: %d\n", val);
            
            // 验证结果（期望值：10 × 50 = 500）
            if(val == NUM_CHILDREN * TARGET_COUNT_PER_CHILD){
                printf(1, "TEST PASSED!\n");
            } else {
                printf(1, "TEST FAILED! Expected %d\n", 
                NUM_CHILDREN * TARGET_COUNT_PER_CHILD);
            }
            
            // ===== 第四部分：清理资源 =====
            
            sem_destroy(SEM_ID);    // 销毁信号量
            unlink(COUNTER_FILE);   // 删除测试文件
            
            return;
        }
        
        int
        main(int argc, char *argv[])
        {
            test_counter();
            return 0;
        }
    \end{codeblock}
    
\end{document}